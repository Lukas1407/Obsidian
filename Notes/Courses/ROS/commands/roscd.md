allows to change directory directly to a package

## Change Dir
`roscd <package>[/subdir]`
Example:
		`roscd roscpp`
		`roscd roscpp/cmake`

## Log
- takes you to the folder where ROS stores its log files